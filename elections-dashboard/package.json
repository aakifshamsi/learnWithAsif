{
  "name": "elections-dashboard",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm --filter @elections/shared build && pnpm -r --filter '!@elections/shared' build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}
